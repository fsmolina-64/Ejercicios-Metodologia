'use strict';

let a = Number(prompt("Ingrese numero"));

let b = Number(prompt("Ingrese numero"));

alert ("a>b: " + (a>b));
alert ("a>=b: " + (a>=b));
alert ("a<b: " + (a<b));
alert ("a<=b: " + (a<=b));
alert ("a==b: " + (a==b));
alert ("a!=b: " + (a!=b));
